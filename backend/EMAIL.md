# Email Backend Archive Notes

The prior Strapi/Resend backend email integration documentation in this file is historical only.

- **Active contact and subscribe forms** now live in Next.js API routes and use Resend directly
- **This backend code is not executed** — do not follow old provider install, configuration, or controller instructions

For current form handling, see:
- `frontend/src/app/api/contact/route.ts`
- `frontend/src/app/api/subscribe/route.ts`
